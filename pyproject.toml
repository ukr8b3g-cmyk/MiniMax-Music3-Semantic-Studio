[project]
name = "minimax-music3-semantic-studio"
version = "1.0.0"
description = "Semantic generation and non-destructive audio editing studio for MiniMax Music 3 in ComfyUI"
requires-python = ">=3.10"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
