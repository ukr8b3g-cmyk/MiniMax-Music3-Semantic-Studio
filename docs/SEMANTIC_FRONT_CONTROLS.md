# Timeline-front generation controls

Semantic Studio keeps only the two frequently adjusted generation values on the Timeline front:

- CFG
- Duration

Music Seed and Top-K remain in Generation. Auto Sync with Timeline remains in Generation and is not promoted to the Timeline front.

The quick controls synchronize with the existing Generation draft before Save to Node, preserving the current node widget and generation contracts.
