# Studio window movement

The shared Studio shell supports title-bar dragging in normal mode and standard drag-to-restore behavior from maximized mode. The last normal position is stored per Studio window key and clamped back into the viewport after display-size changes.

Window control buttons are excluded from the drag surface.
